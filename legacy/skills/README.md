# Legacy StudyOS Skills

These folders are obsolete transition copies of the previous `study-os-*` skills.

They are intentionally outside `skills/` and are not installed by `install_studyos.py` or `sync_studyos.py`. User-facing StudyOS skills now use only the final `studyos-*` names.
